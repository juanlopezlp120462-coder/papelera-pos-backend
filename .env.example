# Copiá este archivo a .env y completá con tus datos reales.
# Si lo dejás vacío, el sistema usa SQLite local (solo para probar en tu PC).

# Ejemplo con PostgreSQL en Railway/Render/Supabase:
# DATABASE_URL=postgresql://usuario:password@host:5432/nombre_db
DATABASE_URL=
